# 📐 付费进群系统 风格指南

## 一、技术规范

### 代码风格
- 保持原生 PHP 多页面架构，不引入重量级框架。
- 样式改造优先集中到公共 CSS，降低页面内联样式耦合。
- 功能逻辑不变、参数不变、数据库结构不变。

### 目录结构
- `admin/`：后台页面和后台接口
- `public/`：前台页面和前台接口
- `pay/`：支付接口适配层
- `config/`：配置和数据库连接
- `static/`：样式、脚本、图片静态资源

### 命名约定
- 页面文件采用语义化蛇形命名（现有命名保持兼容）。
- CSS 类名以现有类名为主，新增类名以功能语义命名。
- 文档命名使用全大写英文+下划线，便于检索。

---

## 二、设计规范（参考 art-design-pro-main）

### 色彩系统
- 主色：`#5356FB`（NFTMAX 主品牌色，主按钮/主操作）
- 强调色：`#F539F8`（关键 CTA 与渐变高亮）
- 辅色：中性灰（文字、边框、背景）
- 语义色：成功绿、失败红、警告橙

### 布局规范
- PC：中间主内容区 + 卡片化结构 + 统一宽度上限
- 手机：保留当前单列布局 + 固定导航 + 触控友好按钮（本次不重构交互）
- 断点建议：
  - `< 992px`：移动端策略
  - `>= 992px`：桌面端策略

### 组件规范
- 导航栏高度、阴影、边框统一
- 菜单卡片、统计卡片、列表项采用一致圆角和间距
- 弹窗宽度随屏幕自适应，不遮挡关键操作按钮

---

## 三、最佳实践

### 响应式改造实践
- 优先加媒体查询覆盖，不直接删除原移动端样式。
- 先保证可用性，再逐步优化视觉统一性。
- 所有节点完成后执行全量回归（登录、设置、订单、审核、访客）。

### 风险控制实践
- 单节点最小改动，防止一次改动影响多个业务链路。
- 每次提交前执行语法、接口可达、关键页面可达测试。
- 文档与代码同步更新，确保可追溯。

---

## 四、节点风格落地记录

### 节点 1：全局样式基线
- 引入后台统一视觉变量：主色、背景色、卡片边框、圆角、阴影。
- 建立桌面端响应式容器宽度规范（`>= 992px`）。
- 保持移动端样式不回退，桌面端仅做增强。

### 节点 2：导航与仪表盘增强
- 后台首页增加运营标识与更清晰的桌面端信息层级。
- 桌面端顶部区使用品牌化渐变，提升“管理后台”感知。
- 保持移动端体验不回退，桌面端增强通过媒体查询注入。

### 节点 3：列表与审核页面增强
- 订单与访客页面在桌面端采用自适应网格卡片布局。
- 审核列表与审核详情通过页面级类名隔离样式作用域。
- 保持移动端单列结构不变，仅在桌面端提高信息密度。

### 节点 4：系统设置入口增强
- 菜单项增加模块说明文案，便于运营快速定位配置入口。
- 桌面端使用双列菜单布局，减少滚动并提升识别效率。
- 移动端隐藏二级描述文本，维持原有简洁体验。

### 节点 5：设置类表单增强
- 桌面端设置页采用大容器 + 明确操作区布局。
- 表单页按钮策略分端处理：移动端固定，桌面端吸附。
- 统一设置类页面卡片边框、圆角与阴影风格。

### 节点 6：模板设置页增强
- 桌面端模板管理区采用宽容器双列布局，提升素材管理效率。
- 预览弹窗采用大尺寸可视区域与统一圆角风格。
- 移动端仍保持紧凑展示与原交互流程。

### 节点 7：模板二设置页增强
- 桌面端金额表单与群组列表采用宽容器分区布局。
- 群组项使用网格结构显示头像、信息和操作按钮。
- 弹窗表单在桌面端使用对齐布局，移动端保留原简洁结构。

### 节点 8：交付一致性校验
- 对所有后台页面完成 PC 增强与移动端保留策略校验。
- 样式新增主要集中在桌面端断点规则，不破坏原移动端体验。
- 文档与代码实现保持一致，支持后续迭代复用。

### 节点 9：稳定性与上线准备
- 修复前台页面外部接口失败时的告警污染，提升线上展示稳定性。
- 补充《上线检查清单》，将部署与验收步骤标准化。

### 节点 10：结构检修与语义一致性
- 修复后台首页和系统设置页中的无效 HTML 结构，避免浏览器自动纠错带来的布局偏差。
- 统一页面语言标识为 `zh-CN`，提升语义一致性与可维护性。
- 坚持“结构修复不改业务”原则，确保功能链路零回归。

### 节点 11：登录页与边缘页面统一
- 登录页采用与后台一致的卡片化视觉与输入反馈，提升 PC 端观感与可读性。
- 错误提示采用条件渲染与转义输出，避免空提示和潜在输出污染。
- 模板一设置页与缓存页完成语义与结构补全，提升页面稳定性与长期维护性。

### 节点 12：返回导航容灾规范
- 后台页面返回按钮统一采用三级容灾：`history.back()` → `document.referrer` → `index.php`。
- 返回按钮默认 `href` 设置为后台首页，确保无脚本场景可用。
- 导航容灾优化不涉及业务逻辑与接口参数变更，确保零风险接入。

### 节点 13：表单文案与提示统一
- 统一设置类页面保存按钮文案，降低运营人员学习成本。
- 为关键字段补充输入示例与风险提示，提升参数录入准确率。
- 在不改业务逻辑前提下增强表单可读性，PC 端更清晰、移动端保持兼容。

### 节点 14：状态与风险提示增强
- 审核设置页引入状态徽标与说明文案，让“开启/关闭”状态可一眼识别。
- 缓存清理页增加目录说明、容量统计卡片和明确确认语句，降低误操作风险。
- 异常场景增加前端提示，避免静默失败影响运营判断。

### 节点 15：敏感操作交互规范
- 删除类操作遵循“明确对象 + 二次确认 + 失败可见反馈”原则。
- 避免同一按钮绑定多重触发路径，确保一次点击仅发起一次请求。
- 对动态参数传值采用安全编码策略，降低脚本拼接风险。

### 节点 16：空态与容灾提示规范
- 对非法入口参数不再直接中断页面，统一转为空态提示和返回入口。
- 后端 POST 分支需做显式条件判断并在响应后及时退出，避免输出串扰。
- 将“错误可见、可回退、可重试”作为审核详情页的最小交互保障。

### 节点 17：列表页可读性规范
- 高频列表页统一增加页面引导文案，降低新运营上手成本。
- 空态从“纯文本”升级为“卡片提示”，并统一分页按钮交互反馈。
- 悬浮敏感操作按钮采用更高对比视觉，提升可发现性并减少误触判断成本。

### 节点 18：敏感操作防重入规范
- 删除/清空类操作需具备“处理中锁 + 按钮禁用态 + 结果反馈”三要素。
- 动态文案优先使用 `textContent`，避免内联 HTML 拼接风险。
- 在网络异常与业务失败场景必须提供用户可见提示，避免静默失败。

### 节点 19：审核链路一致性规范
- 审核更新操作需同时验证记录归属（如 `id + ip`），避免跨分组误操作。
- 当后端未命中记录时，前端需感知失败状态并给出可回退路径。
- 审核详情页在“参数异常/无数据”时统一使用空态提示，不直接中断页面。

### 节点 20：结构一致性规范
- 页面脚本应统一放置在 `body` 闭合前，减少浏览器容错依赖。
- 页面结构调整后需补做关键交互回归，确保行为与视觉反馈保持一致。
- 空态提示需可回退、可重试，保障运营路径连续性。

### 节点 21：脚本稳定性规范
- 页面脚本初始化应避免重复事件绑定，防止一次操作触发多次副作用。
- 动态列表渲染优先采用 DOM API 创建节点，降低字符串拼接风险。
- 提交前应清理历史临时隐藏字段，避免累计污染请求载荷。

### 节点 22：封版验证规范
- 每轮连续改造结束后必须执行全量页面 + 接口回归，形成封版基线。
- 封版节点原则上不新增业务功能，只允许回归修复和文档冻结。
- 交付文档需同步回填最新提交哈希，确保可追溯性完整。

### 节点 23：默认入口一致性规范
- 根路径应提供明确业务入口，不得默认落入异常页。
- 支持通过轻量参数切换前台模板，但默认模板需稳定可访问。
- 入口跳转修复必须保持后台入口与历史路径兼容。

### 节点 24：NFTMAX 视觉统一规范
- 统一主题变量：主色 `#5356FB`、强调色 `#F539F8`、背景 `#ECE9FF`。
- 按钮统一使用品牌渐变（紫->粉），卡片统一大圆角和轻发光阴影。
- 保持“仅改样式不改功能”原则，所有业务脚本与接口协议保持兼容。

### 节点 25：异常信息展示规范
- 外部接口异常信息不直接展示到用户界面，统一改为日志记录。
- 页面展示层优先保证可读性，使用本地兜底文案维持运营连续性。
- 容灾逻辑不得影响原有统计、菜单和业务操作链路。

### 节点 26：桌面端导航布局规范
- 桌面端后台首页采用“左侧功能栏 + 右侧主内容区”双栏结构，提升高频操作效率。
- 左侧功能栏包含品牌区、用户信息区、菜单区与退出区，保证信息层级清晰。
- 移动端继续使用原单列入口结构，避免因桌面布局改造造成触屏交互回退。

### 节点 27：整站壳层一致性规范
- 后台桌面端页面统一注入左侧导航壳层，菜单层级和入口顺序保持一致。
- 首页统计卡片需保证对比度，禁止出现白底白字等低可读性组合。
- 壳层增强必须限定在展示层，避免侵入业务接口与数据流程。

### 节点 28：品牌可配置规范
- 品牌名称、Logo、Favicon 作为独立配置项管理，支持后台可视化修改。
- 上传素材需有格式与体积限制，避免异常文件污染静态资源目录。
- 品牌配置生效范围覆盖后台导航、登录页与前台模板页顶部图标。

### 节点 29：模块化美化收口规范
- 各功能区统一采用卡片式分组（标题/输入区/操作区）并保持一致圆角和边框层级。
- 主操作与危险操作按钮使用统一语义渐变风格，避免视觉冲突。
- 页面优化优先兼容原有业务脚本事件，不允许因样式调整影响功能行为。

### 节点 30：壳层一致性规范
- 后台页面切换不得导致整体布局结构突变，侧栏、主内容区、操作区保持统一骨架。
- 系统设置二级功能统一出现在左侧一级菜单下，避免用户跨页寻找入口。
- 桌面端壳层增强与移动端原生布局需并行兼容。

### 节点 31：操作按钮规范
- 列表页主操作（导出）与风险操作（清空/删除）统一采用胶囊按钮风格。
- 删除入口禁用图标式隐式触发，改为显式文字按钮 + 二次确认文案。
- 导出能力作为列表页标准工具栏能力，保持统一入口位置。


### 节点 32：壳层切换稳定性规范
- 后台左侧菜单（一级 + 系统设置二级）统一采用壳层内异步切换，减少整页刷新闪动。
- 壳层切换期间使用轻量加载态（降透明 + 禁止重复点击），避免布局抖动和重复请求。
- 桌面端侧栏结构保持固定，不因路由变化折叠/展开导致菜单位置跳变。


### 节点 33：系统设置视觉收口规范
- 系统设置全链路页面统一采用“顶部说明 + 卡片模块 + 胶囊按钮”的信息结构。
- 输入控件统一焦点态（主色描边 + 外发光），上传区域统一虚线容器与预览卡样式。
- 设置类弹窗统一使用圆角卡片、遮罩和轻量结果提示，避免旧式分散视觉组件。


### 节点 34：长表单分区折叠规范
- 长表单页面应按业务语义分区（基础信息/素材/内容/评论），每区提供独立折叠入口。
- 折叠交互采用无刷新 class 切换，箭头状态与 `aria-expanded` 同步，确保可访问性。
- 在保持字段协议不变前提下优化信息层级，优先提升运营录入效率与定位速度。


### 节点 35：任务设置信息层级规范
- 任务设置页应按“文案区/素材区/运营提示区”拆分，避免单页信息堆叠。
- 保存动作在移动端使用底部固定主按钮，在桌面端使用右侧吸附主按钮，始终保持高可见性。
- 上传区统一采用“预览 + 虚线上传槽”模式，保证视觉一致与操作预期明确。

### 节点 36：系统设置稳定性与圆角收口规范
- 壳层异步切换场景下，设置页脚本初始化禁止依赖单次 `DOMContentLoaded`，需支持即时初始化并避免重复绑定。
- 系统设置页主操作按钮统一胶囊样式，图片删除等风险操作使用文字按钮，不使用低可见性 `x` 图标。
- 支付设置类页面需提供“可达性验证”入口（网关测试链接）和显式错误反馈，避免配置错误被静默吞掉。

### 节点 37：设置页交互反馈一致性规范
- 配置类页面提交动作必须有“提交中状态 + 成功/失败反馈 + 防重复提交”三要素。
- 异步增删改优先采用结构化响应（JSON），禁止通过匹配 HTML 文案判断操作结果。
- 删除类操作需明确“确认删除/不可恢复”语义，避免隐式触发和模糊反馈。

### 节点 38：顶栏圆角与按钮语义收口规范
- 顶部导航在整站桌面端统一为圆角胶囊结构，避免出现直角或页面间风格突变。
- 模板设置、任务设置、审核状态等系统设置关键页面统一主按钮/次按钮/危险按钮视觉层级与交互反馈。
- 开关类操作需提供即时状态反馈、失败回滚和防重复提交机制，保证运营操作可预期。

### 节点 39：任务设置重构与上传入口统一规范
- 模板页“编辑/预览”双按钮必须同尺寸、同层级，并固定在模板缩略图下方，禁止出现按钮挤压或竖排文本。
- 任务设置页采用“主内容区 + 运营侧栏”双区结构，文案、素材、说明模块分区明确，避免长页面信息混叠。
- 所有上传/选择入口统一为胶囊按钮样式（含原生文件选择按钮兜底），禁止使用方形 `+` 上传块作为主要操作入口。

### 节点 40：admin 入口可达性规范
- 后台未登录跳转必须使用可预测的绝对路径，不允许依赖当前 URL 末尾是否包含斜杠。
- `/admin` 与 `/admin/` 访问行为需保持一致，统一落到后台登录页。

### 节点 41：admin 内部跳转一致性规范
- 后台内部预热与中转页面（如 `groups.php`）的跳转必须使用绝对后台路径，禁止裸相对 `Location`。
- 入口和子链路需保持同域同前缀（`/admin/*`），避免用户在登录态下被误导到根路径页面。

### 节点 42：壳层渲染隔离规范
- 后台壳层路由切换必须同步目标页面样式资源（head）与页面语义状态（body class），禁止跨页样式泄漏。
- 禁止在壳层层面全局强制触发 `window.onload`，页面初始化应采用页面内可重入逻辑并具备防重复绑定机制。
- 错误提示优先使用页面内非阻断反馈，避免在页面切换过程中弹窗打断运营操作链路。

### 节点 43：脚本可重入执行规范
- 后台页面在壳层异步切换场景下必须支持重复进入，禁止使用会触发全局重声明报错的裸 `let/const` 顶层状态变量。
- 页面初始化应具备“页面级绑定锁 + 可重入执行”能力，确保反复切换菜单时功能稳定。
- 控制台零错误作为后台壳层回归验收标准之一，发现 `Identifier ... has already been declared` 必须阻断发布并修复。

### 节点 44：后台接口鉴权一致性规范
- 后台数据接口默认必须登录可访问，禁止未登录状态直接触发读写或清空类操作。
- 后台页面依赖加载顺序遵循“先鉴权、再取数、后渲染”，避免会话状态不一致引发的空态或误请求。

### 节点 45：登录态失效容错规范
- 壳层异步请求链路必须具备“登录页重定向识别”能力，发现会话失效时应立即回到登录页，避免页面停留在半可用状态。
- JSON 接口消费端需补齐解析容错（如 `responseURL` 检测、`try/catch` 兜底），禁止把登录页 HTML 当作 JSON 直接解析。
- 公共静态资源（如 `favicon.ico`）应提供根路径兜底，减少无关 404 噪音对故障排查的干扰。

### 节点 46：全站圆角一致性规范
- 后台桌面端导航系统（顶栏、左侧一级/二级菜单、退出按钮）统一采用胶囊圆角，禁止混用方角视觉。
- 列表页操作按钮应保持最小宽度与统一曲率，确保导出/删除/编辑等高频操作在跨页面下视觉一致。
- 桌面断点下提交类按钮与空态卡片遵循同一圆角体系，避免同页面内出现“圆角风格断层”。

### 节点 47：模板脚本稳健性规范
- 前台通用脚本在不同模板复用时必须做节点存在性判断，禁止直接访问可选 DOM 节点属性导致空指针报错。
- 交互控件（如支付方式单选框）必须保证 `id` 唯一，点击联动逻辑应通过稳定 ID 映射实现。
- 模板预览作为后台高频链路，控制台 `error` 级日志为阻断项，必须在发布前清零。

### 节点 48：壳层切页无闪屏规范
- 后台壳层在路由切换时应采用样式资源增量同步，禁止“先清空全部 CSS 再重建”的高闪屏策略。
- 公共样式资源（如 `admin.css`）应常驻复用，避免因重复注入造成未样式化内容短暂可见。
- 页面专属样式允许按需增删，但需保证切换过程视觉连续，不能出现默认浏览器样式闪现。

### 节点 49：后台滚动交互一致性规范
- 后台统一隐藏滚动条轨道与滑块外观，避免不同页面出现风格割裂的“上下滑动块”。
- 隐藏滚动条仅影响视觉展示，不得影响鼠标滚轮、触控板、键盘滚动行为。
- 规则需同时兼容 WebKit 与 Firefox 内核，保证多浏览器一致表现。

# 🚀 付费进群系统 搭建教程（含宝塔完整流程）

## 1. 部署前准备
### 1.1 服务器建议
- 系统：CentOS 7+/Ubuntu 20+
- 内存：2GB 起（建议 4GB）
- 磁盘：20GB 起
- 带宽：按业务量配置

### 1.2 必要软件版本
- Nginx 或 Apache
- PHP 7.0+（建议 7.4 或 8.x）
- MySQL 5.7+ 或 MariaDB

### 1.3 必要 PHP 扩展
- `mysqli`
- `curl`
- `fileinfo`
- `gd`
- `mbstring`

## 2. 宝塔部署步骤（推荐）
### 2.1 安装宝塔并登录面板
1. 登录服务器，按宝塔官方命令安装
2. 打开宝塔面板地址
3. 完成初始化（账号、密码、端口）

### 2.2 安装运行环境
1. 宝塔 `软件商店` 安装：
- Nginx（或 Apache）
- MySQL / MariaDB
- PHP（建议 7.4）
2. 在 PHP 设置里启用上述扩展

### 2.3 新建站点
1. 进入 `网站 -> 添加站点`
2. 输入域名、站点目录、PHP 版本
3. 同时创建数据库（记下数据库名、用户名、密码）

### 2.4 上传项目源码
1. 进入站点根目录
2. 上传项目源码并解压
3. 确保目录结构完整（`admin/`、`public/`、`config/`、`static/` 等）

### 2.5 导入数据库
1. 进入 `数据库`
2. 选择刚创建的数据库，点 `管理`
3. 导入 `sjk.sql`

### 2.6 修改数据库配置
编辑 `config/config.php`：
```php
$host = 'localhost';
$username = '你的数据库用户名';
$password = '你的数据库密码';
$dbname = '你的数据库名';
```

### 2.7 设置目录权限
重点目录需要可写：
- `upload/`
- `static/images/`

建议权限：
- 目录：`755`
- 文件：`644`

### 2.8 配置 HTTPS（强烈建议）
1. 进入站点 `SSL` 菜单
2. 申请 Let's Encrypt 证书
3. 开启强制 HTTPS

### 2.9 检查站点运行
- 后台：`https://你的域名/admin`
- 前台模板1：`https://你的域名/public/home_v1.php`
- 前台模板2：`https://你的域名/public/home_v2.php`

## 3. 关键业务配置
### 3.1 默认后台账号
- 账号：`admin`
- 密码：`123456`

上线后请立即修改密码。

### 3.2 支付配置
进入后台 `系统设置 -> 支付设置` 填写：
- 支付接口地址
- 商户ID
- 商户密钥
- 回调地址（必须完整可访问）

### 3.3 模板素材配置
进入 `系统设置 -> 模版设置`：
- 替换群二维码
- 替换客服二维码
- 检查预览链接是否正常

## 4. 上线验收清单
### 4.1 基础可用性
- 后台可登录
- 前台可访问
- 模板预览可打开

### 4.2 核心链路
- 用户可提交订单
- 支付回调后订单状态可更新
- 审核流程可通过/驳回

### 4.3 目录与日志
- 上传目录有写权限
- Nginx/PHP 错误日志无致命报错

## 5. 常见问题排查
### 5.1 打开页面空白或 500
- 检查 PHP 版本和扩展
- 查看宝塔 `网站日志` 和 `PHP 错误日志`

### 5.2 数据库连接失败
- 检查 `config/config.php` 参数
- 检查数据库服务是否启动
- 检查账号权限是否正确

### 5.3 支付回调失败
- 回调地址是否可公网访问
- 是否启用了 HTTPS 并证书有效
- 支付参数是否与平台一致

## 6. 安全与运维建议
- 禁用默认后台密码
- 定期备份：数据库 + `upload/` + `static/images/`
- 限制后台登录 IP（可选）
- 定期更新服务器补丁与宝塔版本

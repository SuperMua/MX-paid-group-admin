# 🚀 付费进群系统 搭建教程（含宝塔版，初版）

## 1. 环境要求
- Nginx/Apache
- PHP 7.0+（建议 7.4 或 8.0+，需开启 `mysqli`、`curl`、`gd`）
- MySQL 5.7+ / MariaDB

## 2. 通用部署步骤
1. 上传源码到站点根目录
2. 导入数据库 `sjk.sql`
3. 修改数据库配置 `config/config.php`
4. 后台登录：`/admin`（默认 `admin / 123456`）
5. 配置支付参数和回调地址

## 3. 宝塔面板部署步骤（草案）
1. 新建站点（PHP 项目）
2. 站点目录上传源码
3. 新建数据库并导入 `sjk.sql`
4. 在“软件商店-PHP”中启用扩展：
   - `mysqli`
   - `curl`
   - `fileinfo`
5. 修改 `config/config.php` 数据库连接
6. 设置目录权限：
   - `upload/` 可写
   - `static/images/` 可写
7. 配置伪静态（如使用 Apache 可保留 `.htaccess`）
8. 配置 HTTPS（可选但推荐）
9. 验证后台、前台、支付回调链路

## 4. 验证清单
- 后台登录正常
- 模板预览正常
- 下单与支付回调正常
- 上传与审核流程正常

## 5. 计划补充
- 宝塔界面逐步截图版
- Nginx/Apache 示例配置完整模板

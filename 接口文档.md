# 🔌 付费进群系统 接口文档

## 一、接口概览

### 基础信息
- **API 基础路径**：同域名下 PHP 路由（如 `/admin/*.php`、`/public/*.php`、`/pay/*.php`）
- **认证方式**：后台接口使用 `PHPSESSID` 会话认证；前台接口多为匿名访问
- **请求格式**：`application/x-www-form-urlencoded` / `multipart/form-data` / 查询参数
- **响应格式**：JSON / 纯文本 / 页面跳转（302）

### 通用规范
- 接口与页面混合在 PHP 文件中，需按“是否返回 JSON”区分处理方式。
- 业务改造期间保持入参与出参兼容，不变更字段名。
- 后台写操作必须在登录态下触发，防止未授权访问。

---

## 二、接口清单

### 后台认证与会话
- `POST /admin/login_process.php` - 管理员登录
- `POST /admin/logout.php` - 退出登录
- `GET /admin/login_check.php` - 登录态检查（页面引入）

### 订单与支付配置
- `POST /admin/order-backend.php` - 删除单条/清空订单
- `GET /admin/payment_get.php` - 获取支付配置
- `POST /admin/payment_get.php` - 更新支付配置
- `POST /public/db.php` - 创建前台订单并发起支付
- `GET /pay/notify_url.php` - 支付异步通知
- `GET /pay/return_url.php` - 支付同步返回
- `GET /public/payment.php` - 支付后更新订单状态并跳转

### 审核与任务
- `GET /admin/review_db.php` - 审核数据读取（页面加载）
- `POST /admin/review_db.php` - 审核状态更新/删除/清空
- `GET /admin/settings_backend.php?action=get` - 获取任务设置
- `POST /admin/settings_backend.php` - 更新任务设置
- `POST /public/upload_handler.php` - 前台任务图片上传

### 模板与系统设置
- `GET /admin/settings_crud.php` - 获取模板一设置
- `POST /admin/settings_crud.php` - 更新模板一设置
- `POST /admin/moban.php` - 上传替换二维码/客服图
- `POST /admin/settings_page2.php` - 模板二群组增删改
- `GET /admin/upload_avatar.php?action=get_user_info` - 获取管理员资料
- `POST /admin/upload_avatar.php` - 更新管理员资料

### 访客与状态
- `POST /config/record-visitor.php` - 写入访客记录
- `POST /admin/query-visitors.php` - 清空访客记录
- `GET /admin/auto_status.php` - 获取自动审核状态
- `POST /admin/auto_status.php` - 更新自动审核状态

---

## 三、数据字典

### 订单状态 `orders.payment_status`
- `已支付`：支付完成
- `未支付`：待支付或支付未确认

### 审核状态 `images.status`
- `pending`：待审核
- `approved`：已通过
- `rejected`：未通过

### 支付方式
- `wxpay`：微信
- `alipay`：支付宝

### 自动审核状态
- `1`：开启
- `0`：关闭

---

## 四、节点变更记录

### 节点 1（全局样式基线改造）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：本节点仅调整后台样式，不影响任何 PHP 接口行为

### 节点 2（核心导航与仪表盘 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅调整 `admin/index.php`、`admin/settings.php` 的展示层样式

### 节点 3（订单/访客/审核页面 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅新增页面结构类名与样式规则，不影响数据读写接口

### 节点 4（系统设置入口页 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅增强菜单展示层，不影响模块跳转与数据接口

### 节点 5（设置类表单页 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：表单提交接口保持原路径与参数

### 节点 6（模板设置页 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：二维码替换和预览逻辑保持兼容，仅调整展示层

### 节点 7（模板二设置页 PC 适配）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：群组增删改提交流程与参数保持不变

### 节点 8（全量回归与交付）
- 接口变更：无
- 入参与返回：无变化
- 回归结果：后台关键接口可达性 100%（抽样接口均为 200）

### 节点 9（基线纳管与告警修复）
- 接口变更：无
- 入参与返回：无变化
- 稳定性修复：`public/check.php` 处理外部接口失败时的容错逻辑，避免告警输出

### 节点 10（后台结构检修与语义修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅修复 `admin/index.php`、`admin/settings.php` 的页面结构问题，不影响接口行为

### 节点 11（后台边缘页面检修）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅修复 `admin/login.php`、`admin/settings_page.php`、`admin/upload_cache.php` 展示层与结构问题，认证和数据接口行为不变

### 节点 12（后台返回导航容灾优化）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化后台页面返回按钮策略（history/referrer/index.php），不影响任何接口调用链

### 节点 13（后台表单页可读性精修）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化 `admin/admin_settings.php`、`admin/payment_settings.php`、`admin/settings_frontend.php` 的提示文案与表单展示层，不影响接口行为

### 节点 14（审核与缓存页可用性精修）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化 `admin/audit_status.php`、`admin/upload_cache.php` 的交互提示与展示层，不影响 `auto_status` 与 `upload_avatar` 接口行为

### 节点 15（敏感操作弹窗链路加固）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅增强 `admin/order.php`、`admin/visitor.php`、`admin/review_list.php`、`admin/review_details.php` 的删除确认与错误提示，不影响后端接口协议

### 节点 16（审核详情空态容灾）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化 `admin/review_db.php`、`admin/review_details.php` 的非法参数处理与输出稳定性，不改变审核状态更新和删除接口协议

### 节点 17（高频列表页体验统一）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化 `admin/order.php`、`admin/visitor.php`、`admin/review_list.php` 与 `static/css/admin.css` 的展示层，不影响任何后端接口协议

### 节点 18（敏感操作防重复提交）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅增强 `admin/order.php`、`admin/visitor.php`、`admin/review_list.php` 的前端防重入逻辑与提示文案，不影响后端接口协议

### 节点 19（审核详情链路加固）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：`admin/review_db.php` 仅加强审核状态更新条件（id+ip 归属校验）与错误回流，不改变接口参数与响应协议

### 节点 20（审核交互一致性完善）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅完善 `admin/index.php`、`admin/visitor.php`、`admin/review_list.php` 的页面结构一致性并完成审核链路回归，不影响后端接口协议

### 节点 21（模板一设置脚本稳定性修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅重构 `static/js/settings.js` 前端脚本逻辑，不改变 `settings_crud.php` 的接口协议

### 节点 22（全量终验回归与封版）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：封版节点仅做全量回归验证和文档冻结，不改接口实现

### 节点 23（根路径访问修复）
- 接口变更：`GET /` 入口跳转策略调整
- 入参与返回：新增可选参数 `template|tpl`（支持 `v2|2|home_v2`），其余接口无变化
- 兼容性说明：`/` 默认跳转改为 `/public/home_v1.php`，`/admin` 入口兼容保持不变，未知路径仍跳转错误页

### 节点 24（后台 NFTMAX 风格升级）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅改造后台展示层样式（登录页、首页、审核页、模板设置页），不影响任何接口协议与数据结构

### 节点 25（首页问候语容灾修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：`admin/users.php` 仅优化第三方问候语请求的失败回退逻辑与错误输出策略，不改任何业务接口协议

### 节点 26（首页左侧功能栏布局）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅调整 `admin/index.php` 页面结构与桌面端导航展示，不影响任何接口协议与会话流程

### 节点 27（后台子页面统一壳层）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：新增 `static/js/admin-shell.js` 统一后台桌面端导航壳层，所有接口参数与返回保持兼容

### 节点 28（品牌自定义功能）
- 接口变更：新增 `GET/POST /admin/brand_settings_api.php`
- 入参与返回：
- `GET` 返回 `brand_name/logo_path/favicon_path`
- `POST` 支持 `brand_name` 与文件字段 `logo`、`favicon`（`multipart/form-data`）
- 兼容性说明：新增接口为增量功能，不影响现有业务接口协议

### 节点 29（全页面卡片化收口）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅优化 `admin.css/settings.css/shenhe.css` 展示层，不影响任何接口协议

### 节点 30（整站壳层与二级菜单统一）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅增强 `admin-shell.js` 导航渲染逻辑，不影响业务接口协议

### 节点 31（订单/审核/访客导出能力）
- 接口变更：新增导出接口
- 新增接口：
- `GET /admin/order_export.php` - 导出订单 CSV（Excel 可打开）
- `GET /admin/visitor_export.php` - 导出访客 CSV（Excel 可打开）
- `GET /admin/review_export.php` - 导出审核 CSV（Excel 可打开）
- 兼容性说明：新增能力为增量功能，原删除/审核/分页接口协议不变


### 节点 32（左侧导航无闪刷切换）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅增强 `static/js/admin-shell.js` 与 `static/css/admin.css` 的前端壳层切换策略（异步局部渲染 + 过渡态），业务接口协议保持不变


### 节点 33（系统设置页面美化收口）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅重构系统设置相关页面样式与前端交互细节，接口路径、请求参数、响应结构保持不变


### 节点 34（模板一设置分区折叠）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅调整 `settings_page.php` 页面结构与 `settings.css/settings.js` 前端交互，`settings_crud.php` 接口协议保持不变


### 节点 35（任务设置页面重构美化）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：仅重构 `settings_frontend.php` 展示层，不改变 `settings_backend.php` 的请求字段与响应结构

### 节点 36（系统设置功能回归修复与支付链路加固）
- 接口变更：无新增接口，`POST /admin/payment_get.php` 增加服务端 URL 规范化与格式校验
- 入参与返回：
- 入参保持不变：`api_url`、`merchant_id`、`secre_key`、`callback_url`
- 成功返回保持不变：`更新成功`
- 新增错误提示场景：支付地址/回调地址格式错误时返回明确中文错误文案（HTTP 422）
- 兼容性说明：`pay/lib/pay.php` 增加网关缺省提示与回调地址兜底，不影响原支付参数字段与回调协议

### 节点 37（系统设置深度美化与模板二交互稳定性加固）
- 接口变更：`POST /admin/settings_page2.php` 新增 Ajax JSON 响应模式（仅当请求头携带 `X-Requested-With: XMLHttpRequest`）
- 入参与返回：
- 入参保持不变：`action`、`id`、`nickname`、`content`、`avatar`
- 新增 Ajax 返回：`{ success: boolean, message: string }`
- 非 Ajax 请求兼容保留：页面渲染流程不变，支持原有表单提交方式
- 兼容性说明：为增量兼容改造，不影响模板二原有字段协议与数据库结构

### 节点 38（模板按钮与任务设置/审核状态圆角化收口）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `admin/moban.php` 仅调整按钮结构与样式，不改变预览/上传/跳转路径
- `admin/settings_frontend.php` 仅调整页面布局和字段分区，不改变 `settings_backend.php` 入参字段
- `admin/audit_status.php` 仅增强前端交互反馈与防重复提交，不改变 `auto_status.php` 接口协议

### 节点 39（模板按钮等宽与任务设置重构、上传控件胶囊化）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `admin/settings_frontend.php` 仅重构展示层与上传交互样式，`settings_backend.php` 字段协议保持不变
- `admin/moban.php` 仅调整模板操作按钮布局，不影响模板编辑/预览链路
- 上传相关页面（`settings_page.php`、`settings_page2.php`、`admin_settings.php`、`brand_settings.php`）仅更新按钮样式和触发方式，文件上传字段与后端处理逻辑保持兼容

### 节点 40（admin 入口跳转修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `admin/login_check.php` 仅修复未登录跳转地址生成逻辑
- 后台入口 `/admin` 与 `/admin/` 均统一跳转至 `/admin/login.php`，不影响登录接口参数与会话机制

### 节点 41（admin 内部链路跳转修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `admin/users.php` 与 `admin/groups.php` 仅修复后台内部重定向路径生成，不涉及数据查询参数变更
- 保障后台内部跳转始终留在 `/admin/*` 路径空间，避免误跳转根路径 `/groups.php`

### 节点 42（壳层串页与缓存提示误报修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `static/js/admin-shell.js` 仅优化后台前端壳层路由渲染与资源同步机制，不变更任何业务接口协议
- `admin/index.php`、`admin/review_details.php`、`admin/upload_cache.php` 仅调整页面初始化时机和错误提示方式，不变更请求参数或响应结构

### 节点 43（脚本重复注入稳定性加固）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `static/js/admin-shell.js` 仅增强 head 资源首轮管理与页面脚本重复执行稳定性，不改变接口路径和字段
- `visitor.php`、`review_list.php`、`order.php`、`review_details.php`、`audit_status.php`、`brand_settings.php` 仅调整前端脚本初始化方式，不改变后端协议

### 节点 44（后台接口鉴权补齐）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `query-visitors.php`、`settings_backend.php`、`settings_crud.php`、`upload_avatar.php` 新增登录鉴权拦截，未登录访问统一跳转 `/admin/login.php`
- `users.php`、`groups.php` 作为后台内部链路页面补齐鉴权，不影响登录态下的数据预热行为

### 节点 45（登录态失效容错与导航健壮性加固）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `static/js/admin-shell.js` 新增前端异步请求登录态守卫；当请求被重定向到登录页时自动跳转 `/admin/login.php`，不改变任何接口协议
- `static/js/settings.js`、`admin/audit_status.php` 仅补充 `responseURL` 检测和 JSON 解析容错，不修改接口路径、参数与响应字段
- 新增根路径 `favicon.ico` 仅用于资源兜底，不涉及业务接口链路

### 节点 46（后台圆角胶囊化视觉收口）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- 本节点仅调整 `static/css/admin.css` 视觉样式（导航、按钮、空态卡片圆角与尺寸一致性），不涉及任何接口协议变更
- 所有后台接口路径、请求参数与响应结构保持完全兼容

### 节点 47（模板预览脚本容错与支付方式切换修复）
- 接口变更：无
- 入参与返回：无变化
- 兼容性说明：
- `static/js/script.js` 仅补齐前端空节点容错，不修改任一接口路径与请求协议
- `public/home_v1.php` 仅修复支付方式单选框 `id` 唯一性（`wxpay`/`alipay`），`payment_method` 提交字段保持不变

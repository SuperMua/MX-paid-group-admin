# 🛠 付费进群系统 技术使用指南

## 1. 适用对象
- 后端开发工程师
- 运维工程师
- 二次开发维护人员

## 2. 系统技术概览
### 2.1 架构
- 架构类型：单体 PHP 多页面应用（MPA）
- 运行模式：Web Server + PHP + MySQL/MariaDB
- 状态管理：PHP Session
- 支付链路：易支付接口（同步回调 + 异步回调）

### 2.2 目录说明
- `admin/`：后台页面与后台处理接口
- `public/`：前台页面与用户流程接口
- `pay/`：支付适配层
- `config/`：数据库和访问配置
- `static/`：样式、脚本、图片资源
- `upload/`：前台任务上传文件目录

## 3. 环境要求
- PHP：7.0+（建议 7.4/8.x）
- MySQL：5.7+ 或 MariaDB
- Web 服务：Nginx 或 Apache
- 必须启用扩展：`mysqli`、`curl`、`fileinfo`、`gd`

## 4. 本地开发与启动
### 4.1 数据库初始化
```powershell
# 创建数据库并导入
"C:\\Program Files\\MariaDB 12.2\\bin\\mariadb.exe" -u root -e "CREATE DATABASE IF NOT EXISTS qun555 DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
cmd /c "\"C:\\Program Files\\MariaDB 12.2\\bin\\mariadb.exe\" -u root qun555 < \"sjk.sql\""
```

### 4.2 配置数据库连接
- 修改 `config/config.php`：
- `$host`
- `$username`
- `$password`
- `$dbname`

### 4.3 启动服务
```powershell
# 启动 PHP 开发服务器（项目根目录）
php -S 127.0.0.1:8000
```

### 4.4 访问地址
- 后台登录页：`http://127.0.0.1:8000/admin/login.php`
- 前台模板1：`http://127.0.0.1:8000/public/home_v1.php`
- 前台模板2：`http://127.0.0.1:8000/public/home_v2.php`

## 5. 开发规范（本项目）
- 业务逻辑优先稳定：前后端参数与数据库结构不随意改动
- 样式改造优先：PC 端通过媒体查询增强，移动端不回退
- 每个开发节点必须执行：
- 语法检查
- 页面可达测试
- 接口可达测试
- 性能抽样测试
- 中文 Git 提交

## 6. 常用测试命令
```powershell
# 语法检查（全量）
Get-ChildItem admin,public,config,pay -Recurse -Filter *.php | ForEach-Object { php -l $_.FullName }

# 登录并检测页面
curl.exe -c cookie.txt -d "name=admin&password=123456" http://127.0.0.1:8000/admin/login_process.php
curl.exe -L -b cookie.txt -o NUL -w "%{http_code}" http://127.0.0.1:8000/admin/index.php
```

## 7. 关键链路说明
### 7.1 登录链路
- `admin/login.php` -> `admin/login_process.php` -> `admin/groups.php` -> `admin/index.php`

### 7.2 支付链路
- 前台下单：`public/db.php`
- 统一支付发起：`pay/lib/pay.php`
- 支付同步返回：`pay/return_url.php`
- 支付异步通知：`pay/notify_url.php`
- 订单状态更新：`public/payment.php`

### 7.3 审核链路
- 前台上传：`public/upload_handler.php`
- 后台审核列表：`admin/review_list.php`
- 后台审核详情：`admin/review_details.php`
- 审核状态更新：`admin/review_db.php`

## 8. 故障排查手册
### 8.1 后台无法登录
- 检查 `config/config.php` 数据库配置
- 确认 `admin` 表存在并包含管理员账号
- 检查 PHP Session 是否可写

### 8.2 上传失败
- 检查 `upload/`、`static/images/` 目录写权限
- 检查 `upload_max_filesize`、`post_max_size`
- 检查 `fileinfo` 扩展是否启用

### 8.3 支付不回调
- 检查支付平台回调地址是否公网可访问
- 检查 `payment` 表中接口配置是否完整
- 检查 HTTPS 与证书配置是否正确

## 9. 二次开发建议
- 先在 `接口文档.md` 标注接口变更，再改代码
- 尽量复用现有目录与命名规范
- 修改支付相关逻辑前先做沙箱联调
